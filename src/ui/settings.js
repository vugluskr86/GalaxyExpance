/** Глобальные настройки вида (не входят в зерно вселенной). */
export const settings = {
  dust: 0.7,
  lod: 1,
  rot: 0.01,
  twinkle: true,
  labels: true,
  speed: 1     // 0 = пауза, 1× = норма, 2× / 5× / 10× — ускорение
};
