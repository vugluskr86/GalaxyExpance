; PCOS installer bootstrap.  The setup session is supplied by the firmware
; after this protected PCVM binary has started from a PCFD installation disk.
.protected
PRINT "PCOS INSTALLER 1.0"
PRINT "Preparing installation session..."
HALT
